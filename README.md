# Inventory-Management-System
A data strucutre and algorithm project attempting to implement data structure for actual usage for managing invetory through CLI

## How to run

```bash
g++ **/*.cpp -o inventory.exe
./inventory.exe
```